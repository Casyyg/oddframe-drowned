# Copy to .env locally if needed. Never commit credentials.
DB_DRIVER=sqlite
DB_PATH=data/oddframe.sqlite
HOST=127.0.0.1
PORT=8787
# AWS runtime also requires DB_DRIVER=dynamodb, DYNAMODB_TABLE, AWS_REGION,
# NODE_ENV=production, ADMIN_TOKEN (32+ random characters), ORIGIN_SECRET,
# and PUBLIC_ORIGIN=https://your-cloudfront-domain.
